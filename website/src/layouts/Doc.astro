---
import Base from './Base.astro';

const { title, description } = Astro.props.frontmatter ?? Astro.props;
---

<Base title={`${title} · TimeCheese`} description={description}>
  <article class="prose-doc max-w-2xl mx-auto px-6 py-16">
    <slot />
  </article>
</Base>
